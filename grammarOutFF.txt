

First集算法：
First(S) = First(ar) + First(b)
First(A) = First(e)

First集：
First(S)={ar,b}
First(A)={e}

Follow集算法：
Follow(S):	S→S		
Follow(A):	S→arfAdw;		S→bAc;		

Follow集：
Follow(S)={ε}
Follow(A)={dw,c}