

First集算法：
First(S) = First(ag) + First(b)
First(A) = First(e)

First集：
First(S)={ag,b}
First(A)={e}

Follow集算法：
Follow(S):	S→S		
Follow(A):	S→agfAd;		S→bAc;		

Follow集：
Follow(S)={ε}
Follow(A)={d,c}